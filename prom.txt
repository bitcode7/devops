# Nginx Monitoring with Prometheus & Grafana (Ubuntu)

## Step 1 — Update Ubuntu Packages

```bash
sudo apt update
```

# Refreshes Ubuntu package list

---

# Step 2 — Install Required Packages

```bash
sudo apt install -y ca-certificates curl gnupg
```

# Installs required dependencies

---

# Step 3 — Create Docker Keyrings Directory

```bash
sudo install -m 0755 -d /etc/apt/keyrings
```

# Creates secure directory for Docker keys

---

# Step 4 — Download Docker GPG Key

```bash
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
```

# Downloads Docker security key

---

# Step 5 — Add Docker Repository

```bash
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

# Adds official Docker repository to Ubuntu

---

# Step 6 — Update Packages Again

```bash
sudo apt update
```

# Refreshes package list including Docker repo

---

# Step 7 — Install Docker + Docker Compose

```bash
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
```

# Installs:
# - Docker Engine
# - Docker CLI
# - Container runtime
# - Docker Compose plugin

---

# Step 8 — Add User to Docker Group

```bash
sudo usermod -aG docker $USER
```

# Allows Docker usage without sudo

---

# Step 9 — Apply Group Changes

```bash
newgrp docker
```

# Activates Docker group permissions immediately

---

# Step 10 — Verify Docker Installation

```bash
docker --version
```

# Displays Docker version

---

# Step 11 — Create Project Folder

```bash
mkdir ~/nginx-monitoring
cd ~/nginx-monitoring
```

# Creates project directory

---

# Step 12 — Create Prometheus Configuration

```bash
nano prometheus.yml
```

Add:

```yaml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "nginx"
    static_configs:
      - targets: ["nginx-exporter:9113"]
```

Save:
- Ctrl + X
- Y
- Enter

# Configures Prometheus scraping targets

---

# Step 13 — Create Nginx Configuration

```bash
nano nginx.conf
```

Add:

```nginx
events {}

http {
  server {
    listen 80;

    location /nginx_status {
      stub_status on;
      allow all;
    }
  }
}
```

Save:
- Ctrl + X
- Y
- Enter

# Enables Nginx metrics endpoint

---

# Step 14 — Create Docker Compose File

```bash
nano docker-compose.yml
```

Add:

```yaml
version: '3'

services:
  prometheus:
    image: prom/prometheus
    container_name: prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    container_name: grafana
    ports:
      - "3000:3000"

  nginx:
    image: nginx
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf

  nginx-exporter:
    image: nginx/nginx-prometheus-exporter
    container_name: nginx-exporter
    command:
      - -nginx.scrape-uri=http://nginx/nginx_status
    ports:
      - "9113:9113"
    depends_on:
      - nginx
```

Save:
- Ctrl + X
- Y
- Enter

# Defines all monitoring services

---

# Step 15 — Start All Containers

```bash
docker compose up -d
```

# Starts:
# - Prometheus
# - Grafana
# - Nginx
# - Nginx Exporter

---

# Step 16 — Check Running Containers

```bash
docker compose ps
```

# Displays running services

---

# Step 17 — View Logs

```bash
docker compose logs -f
```

# Displays live container logs

---

# Step 18 — Find Ubuntu IP Address

```bash
hostname -I
```

# Displays system IP address

---

# Step 19 — Open Services in Browser

## Prometheus

```text
http://your-ip:9090
```

# Opens Prometheus dashboard

---

## Grafana

```text
http://your-ip:3000
```

Login:

```text
Username: admin
Password: admin
```

# Opens Grafana dashboard

---

## Nginx

```text
http://your-ip:80
```

# Opens Nginx webpage

---

# Step 20 — Connect Grafana to Prometheus

Inside Grafana:
- Go to:
  ```text
  Connections → Data Sources
  ```

- Click:
  ```text
  Add data source
  ```

- Choose:
  ```text
  Prometheus
  ```

URL:

```text
http://prometheus:9090
```

- Click:
  ```text
  Save & Test
  ```

# Connects Grafana with Prometheus

---

# Step 21 — Generate Test Traffic

```bash
for i in {1..50}; do curl http://localhost & done
```

# Sends multiple requests to Nginx

---

# Step 22 — Import Grafana Dashboard

Inside Grafana:
- Go to:
  ```text
  Dashboards → New → Import
  ```

Enter Dashboard ID:

```text
12708
```

- Click:
  ```text
  Load
  ```

- Select:
  ```text
  Prometheus
  ```

- Click:
  ```text
  Import
  ```

# Imports Nginx monitoring dashboard

---

# Step 23 — Stop All Containers

```bash
docker compose down
```

# Stops and removes all containers

---

# Quick Reference Commands

## Start Containers

```bash
docker compose up -d
```

# Starts all services

---

## Stop Containers

```bash
docker compose down
```

# Stops all services

---

## Check Status

```bash
docker compose ps
```

# Shows running containers

---

## View Logs

```bash
docker compose logs -f
```

# Displays live logs

---

## Find System IP

```bash
hostname -I
```

# Displays Ubuntu IP address

---

# Important Concepts

| Concept | Meaning |
|----------|----------|
| Docker Compose | Runs multiple containers together |
| Prometheus | Metrics collection system |
| Grafana | Monitoring dashboard |
| Nginx Exporter | Converts Nginx stats into Prometheus metrics |
| scrape_interval | How often Prometheus collects metrics |
| Dashboard | Visualization panel |

---

Step 6 — Open in Your Browser
ServiceURLLoginPrometheushttp://localhost:9090none neededGrafanahttp://localhost:3000admin / adminNginxhttp://localhost:80none needed

Step 7 — Connect Grafana to Prometheus
In Grafana:

Left sidebar → Connections → Data Sources
Click Add data source
Choose Prometheus
Set URL to: http://prometheus:9090
Click Save & Test — should say "Successfully queried"


Grafana needs to know where to pull data from. You're pointing it at the Prometheus container using its internal Docker network name (prometheus), not localhost.


Step 8 — Generate Nginx Traffic (to see metrics)
bashfor i in {1..50}; do curl http://localhost & done

Sends 50 HTTP requests to Nginx in parallel. The & runs each curl in the background so they all fire at once. This gives Prometheus real traffic data to collect, so your Grafana graphs actually show something instead of flat lines.


Step 9 — Import a Grafana Dashboard

Left sidebar → Dashboards → New → Import
In the "Import via grafana.com" box, type 12708 and click Load
Select your Prometheus data source from the dropdown
Click Import


Dashboard ID 12708 is an official Nginx dashboard — it auto-creates panels for active connections, request rates, and more.

Alternatively, you can add custom panels manually with these PromQL queries:

Active connections: nginx_connections_active
Total requests: nginx_http_requests_total


Step 10 — Stop Everything When Done
bashdocker compose down

Stops and removes all running containers. Your config files (prometheus.yml, nginx.conf, docker-compose.yml) stay intact — just run docker compose up -d again to bring it all back.


Quick Reference
ActionCommandStart all containersdocker compose up -dStop all containersdocker compose downCheck container statusdocker compose psView logsdocker compose logs -fGenerate test trafficfor i in {1..50}; do curl http://localhost & done
